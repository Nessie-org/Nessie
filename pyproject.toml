[project]
name = "nessie"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []


[project.entry-points.nessie_plugins]
plugin_example = "demo_plugin:plugin"

[dependency-groups]
dev = [
    "mypy>=1.19.1",
]
